---
import ProLayout from '../layouts/ProLayout.astro';
---

<ProLayout title="nannomnom">
  <section style="
    padding: var(--space-10) var(--space-6);
    text-align: center;
  ">
    <h1 style="
      font-family: var(--font-display);
      font-size: clamp(2rem, 6vw, 4rem);
      font-weight: 700;
      color: var(--fg);
      margin: 0;
    ">
      Tokens loaded — nannomnom
    </h1>
  </section>
</ProLayout>
